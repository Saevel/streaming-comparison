group 'org.saevel'

version '1.0'

allprojects {
    ext {
        scalaVersion = [
                "major": "2.12",
                "minor": 8
        ]

        spark = [
                "version": "2.4.0"
        ]

        flink = [
                "version": "1.8.0"
        ]

        kafkaStreams = [
                "version": "2.2.0"
        ]
        
        akkaStreams = [
                "version": "1.0.1"
        ]
    }
}